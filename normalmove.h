#ifndef NORMALMOVE_H
#define NORMALMOVE_H

#include <string>
#include "move.h"
#include "piece.h"
#include "board.h"

class normalMove : public Move {
public:
    // normalMove(Board *b, Position start, Position end, std::string colour);
    // Board movePiece(std::string promoteTo = 0) override;
};

#endif
